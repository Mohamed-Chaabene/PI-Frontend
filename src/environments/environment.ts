export const environment = {
  production: false,
  /** Base path for HTTP API (dev server uses proxy to Spring). */
  apiUrl: '/api',
  cloudinary: {
    cloudName: 'dlmeofspy',
    uploadPreset: 'testtest'
  }
};
